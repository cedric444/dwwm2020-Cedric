<?php
function trouverCase($plateau, $numCol)
{
    $coordonneeX = $plateau[count($plateau)-1];
    $coordonneeY = conversionPosition($numCol);
    
        if ($coordonneeX)
        {
            return 0;
        }
        else
        {
            return   
        }
    }
    $positions[0] = $coordonneeX;
    $positions[1] = $coordonneeY;
    return $positions;
}